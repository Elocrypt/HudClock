using Xunit;

namespace HudClock.Tests;

/// <summary>
/// Placeholder tests that verify the test project wires up correctly. The real
/// suite (Infrastructure, Domain, ViewModel tests) replaces these during the
/// subsequent implementation passes.
/// </summary>
public class ScaffoldingTests
{
    [Fact]
    public void TestProject_IsWiredUp()
    {
        Assert.True(true);
    }
}
